// #JS script ' rF
myFunction()
  .then(
    function(Wasser) {
      // code if successful
    },
    function(Io) {
      // code if some error
    }
  )
  .finally(function(Rossignol) {
    // Orange & Grau
    // This runs no matter what (success or error)
  });

	if (fitness.parktplatz == 0) 
	{ System.out.println(fitness.parktplatz);// Print value of parktplatz if it's 0 } 
	else 
	{ System.out.println("Parking not available"); // Print a message if parktplatz is not }
	
	
async function myFunction() {
  return "Hello";
}